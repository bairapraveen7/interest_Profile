<h3>Review: <%= @user.food_review(@food) %>
</h3>

<% if current_user?(@user) %>

    <%= form_with(url: user_food_path(@user,@food,review: true), method: "patch" , local: false) do %>
        <%= submit_tag "Modify review" , class: "btn btn-primary" %>
            <% end %>

                <% end %>
