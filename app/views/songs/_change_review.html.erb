
<%= form_with(url: user_song_path(current_user,@song, updated: true), method: "patch", local: false ) do |f| %>
            <div class="form-group row">
                <%= f.label :review, "Review" , class: "col-sm-2 col-form-label" %>
                    <div class="col-sm-10">
                        <%= f.text_area :review,value: current_user.connect_songs.find_by(song_id: @song.id).review, class: "form-control" %>
                    </div>
            </div>
            <%= f.submit "Update Details" , class: "btn btn-primary" %>
        <% end %>

    </div>
</div>