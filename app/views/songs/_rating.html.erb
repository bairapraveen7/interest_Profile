<h3>Rating: <%= @user.song_rating(@song) %>
</h3>

<% if current_user?(@user) %>

    <%= form_with(url: user_song_path(@user,@song,rating: true), method: "patch" , local: false) do %>
        <%= submit_tag "Modify rating" , class: "btn btn-primary" %>
            <% end %>

                <% end %>