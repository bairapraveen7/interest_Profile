<h3>Review: <%= @user.movie_notes(@movie) %>
</h3>

<% if current_user?(@user) %>

    <%= form_with(url: user_movie_path(@user,@movie,review: true), method: "patch" , local: false) do %>
        <%= submit_tag "Modify review" , class: "btn btn-primary" %>
            <% end %>

                <% end %>
