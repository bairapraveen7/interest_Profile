<%= form_with(model: current_user.active_relationships.build, class: "col-8", local: false) do %>
   <%= hidden_field_tag :other_user, other_user_id %>
   <%= hidden_field_tag :interest, interest %>
   <%= submit_tag "follow", class: "btn btn-primary " %>
<% end %>