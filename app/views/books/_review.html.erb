<h3>Review: <%= @user.book_review(@book) %>
</h3>

<% if current_user?(@user) %>

    <%= form_with(url: user_book_path(@user,@book,review: true), method: "patch" , local: false) do %>
        <%= submit_tag "Modify review" , class: "btn btn-primary" %>
            <% end %>

                <% end %>
